package uids

// graph.go is dependency-graph image tooling: the blank imports below are
// the inputs to the workspace-wide module graph generator. Each provider
// sub-module is intentionally NOT blank-imported here — they live in
// separate Go modules with their own dependency closures.
