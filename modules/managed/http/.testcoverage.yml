# (mandatory)
# Path to coverprofile file (output of `go test -coverprofile` command).
profile: .reports/testcoverage.out

# (optional; but recommended to set)
# When specified, reported file paths will not contain local prefix in the output
local-prefix: "github.com/guidomantilla/yarumo/managed/http"

# Holds coverage thresholds percentages, values should be in range [0-100]
threshold:
  # (optional; default 0)
  # The minimum coverage that each file should have.
  # Lowered to accommodate server.go: the TLS branch and the Stop
  # timeout fallback (lines that close the listener + force Close after
  # http.Server.Shutdown returns an error) are difficult to exercise in
  # unit tests without an in-memory cert and a hanging handler.
  file: 75

  # (optional; default 0)
  # The minimum coverage that each package should have
  package: 80

  # (optional; default 0)
  # The minimum total coverage a project should have
  total: 85
