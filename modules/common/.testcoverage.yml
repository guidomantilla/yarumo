# (mandatory)
# Path to coverprofile file (output of `go test -coverprofile` command).
#
# For cases where there are many coverage profiles, such as when running
# unit tests and integration tests separately, you can combine all those
# profiles into one. In this case, the profile should have a comma-separated list
# of profile files, e.g., 'cover_unit.out,cover_integration.out'.
profile: .reports/testcoverage.out

# (optional; but recommended to set)
# When specified, reported file paths will not contain local prefix in the output
local-prefix: "github.com/guidomantilla/yarumo/common"

# Holds coverage thresholds percentages, values should be in range [0-100]
threshold:
  # (optional; default 0)
  # The minimum coverage that each file should have
  file: 90

  # (optional; default 0)
  # The minimum coverage that each package should have
  package: 90

  # (optional; default 0)
  # The minimum total coverage a project should have
  total: 99

# Holds regexp rules which will override thresholds for matched files or packages
# using their paths.
#
# The first rule from this list that matches a file or package is going to apply
# a new threshold to it. If a project has multiple rules that match the same path,
# override rules should be listed in order from specific to more general rules.
override:
  # uids/functions.go: the uncovered branches are the error paths of
  # uuid.NewRandom, nanoid.New and uuid.NewV7, which only return errors when
  # the underlying crypto/rand source fails. Not reachable from unit tests
  # without mocking the rand source upstream.
  - threshold: 85
    path: ^uids/functions\.go$

# Holds regexp rules which will exclude matched files or packages
# from coverage statistics
exclude:
#  # Exclude files or packages matching their paths
  paths:
    - ^diagnostics/examples/main.go
    - ^health/examples/main.go
    - ^resilience/examples/main.go
    - ^validation/examples/main.go
    # lifecycle/tests/ is a test-only helper subpackage. Its failure
    # branches (panic recovery, goroutine-leak path, ctx-timeout path)
    # are exercised only when a Component implementation breaks the
    # contract, which is by design not reproducible from a unit test
    # of the helper itself.
    - ^lifecycle/tests/

# NOTES:
# - symbol `/` in all path regexps will be replaced by the current OS file path separator
#   to properly work on Windows
